name: cli
publish_to: none
description: An interactive command-line client for searching and reading Wikipedia.
version: 1.0.0
resolution: workspace

executables:
  dartpedia: cli

environment:
  sdk: ^3.12.2

dependencies:
  path: ^1.9.0
  http: ^1.4.0
  logging: ^1.2.0
  command_runner:
    path: ../command_runner
  wikipedia:
    path: ../wikipedia
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
